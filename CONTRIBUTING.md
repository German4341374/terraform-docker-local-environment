# Contributing

Create a branch, run `make setup`, then `make lint`, `make validate`, `make test`, and `make plan`.
Review plans for replacement or deletion before applying. Include variable, state, security, and rollback
impact in pull requests. Never commit state, plans, credentials, or environment-specific passwords.
Use imperative English commit messages.
