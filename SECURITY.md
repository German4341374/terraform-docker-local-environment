# Security Policy

Report vulnerabilities through GitHub private security advisories. State and plan files can contain
passwords even when Terraform marks values sensitive; store them only locally with restrictive file
permissions and never attach binary plans to issues. PostgreSQL has no published host port, the proxy
binds only to loopback, images are explicitly tagged, and application/proxy containers drop capabilities.
This local demonstration is not an internet-facing production platform.
